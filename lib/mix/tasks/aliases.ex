# This file is for adding short docs for mix aliases so they show up in the mix help command output.
defmodule Mix.Tasks.Watch do
  use Mix.Task
  @shortdoc "Alias for import.env and phx.server"
  def run(_) do
  end
end
